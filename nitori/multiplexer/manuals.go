package multiplexer

import (
	"github.com/bwmarrin/discordgo"
)

func (mux *Multiplexer) Manuals(session *discordgo.Session, message *discordgo.Message, context *Context) {
	//var err error
	//guildID, guildPrefix := config.GeneratePrefixStuff(context, message)

}
